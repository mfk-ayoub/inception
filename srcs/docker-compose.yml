services:

  nginx:
    image: nginx
    container_name: nginx
    build: ./requirements/nginx
    ports:
      - "443:443"
    restart: always
    networks:            
      - inception 


networks:
  inception:
    driver: bridge
    name: inception

