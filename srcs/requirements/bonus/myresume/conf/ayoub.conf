server
{
        listen  4242;
        server_name localhost;

        root /var/www/html;
        index index.html;
        location / {
            root /var/www/html;
            index index.html;
        }
}

